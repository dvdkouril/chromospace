import type { PerspectiveCamera } from 'three';

export class ChromatinRenderer {
    
    render(delta: number, scene: ChromatinScene, camera: PerspectiveCamera) {
        console.log("i'm rendering...");
    }
}